---
phase: 04-pipeline-integration-and-output
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - app/page.tsx
  - components/upload/ManualUpload.tsx
  - components/pipeline/PipelineStatus.tsx
  - components/pipeline/VideoResult.tsx
  - app/api/jobs/route.test.ts
autonomous: true
requirements:
  - OUTPUT-01
user_setup:
  - "Dev server + Phase 3 worker or mocked videoUrl for visual smoke"
must_haves:
  truths:
    - "Pipeline section uses honest single-row processing when stages are null (D-01)"
    - "Video section uses native video controls and 16:9 container per 04-UI-SPEC"
    - "On completed + videoUrl, focus moves to video region (D-02)"
    - "On failed, destructive alert emphasizes uploading another manual (D-03)"
  artifacts:
    - path: components/pipeline/PipelineStatus.tsx
      provides: Processing UI
    - path: components/pipeline/VideoResult.tsx
      provides: HTML5 player + placeholder
  key_links:
    - from: components/upload/ManualUpload.tsx
      to: app/api/jobs/[id]/route.ts
      via: fetch GET
      pattern: videoUrl, stages
---

<objective>
Implement **Phase 4 product UI**: **`PipelineStatus`** (honest coarse progress when `stages === null`), **`VideoResult`** (16:9 `<video controls>` or placeholder), integrate into **`ManualUpload`** with **`aria-live="polite"`**, **smooth scroll + focus** on success (D-02), failure copy per D-03 and **`04-UI-SPEC.md`**. Adjust **`app/page.tsx`** layout so the **player** may use **up to 960px** width while **title + upload** stay **640px** centered.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pipeline-integration-and-output/04-UI-SPEC.md
@.planning/phases/04-pipeline-integration-and-output/04-CONTEXT.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@components/upload/ManualUpload.tsx
@app/page.tsx
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| XSS via `error` or `jobError` | medium | Render error text as plain text nodes; do not use `dangerouslySetInnerHTML` |
| Misleading pipeline progress | low | D-01: no fake stage checkmarks when `stages` is null |

**ASVS:** L1 — client-only display; API already sanitizes server errors.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: PipelineStatus component</name>
  <files>components/pipeline/PipelineStatus.tsx</files>
  <read_first>
  - components/upload/ManualUpload.tsx
  - .planning/phases/04-pipeline-integration-and-output/04-UI-SPEC.md
  </read_first>
  <action>
  Add **`components/pipeline/PipelineStatus.tsx`** exporting:

  ```tsx
  export function PipelineStatus(props: {
    stages: null | Array<{
      id: string;
      label: string;
      state: "pending" | "active" | "complete" | "failed";
    }>;
    coarseStatus: "queued" | "processing" | "completed" | "failed";
  }): JSX.Element
  ```

  **Behavior:**
  - Section title: **`Processing your manual`** (exact string from UI-SPEC copy table).
  - If **`props.stages === null`** and job not terminal: show **one** row with **`Loader2`** spinner + copy **`Working on your video…`** (exact).
  - If **`props.stages === null`** and **`coarseStatus === "queued"`**: show queued-appropriate single-line (e.g. spinner + “Waiting to start…” or match existing ManualUpload tone — must not show 5 fake steps).
  - If **`props.stages`** is a non-empty array: render ordered list with lucide icons per row state (implement minimal: pending/active/complete/failed).
  - Wrap in **`role="region"`** and **`aria-live="polite"`** on the status container.

  Use **`Card`** or bordered **`div`** + Tailwind consistent with Phase 1.
  </action>
  <acceptance_criteria>
  - File exists at **`components/pipeline/PipelineStatus.tsx`**
  - `grep -q "Processing your manual" components/pipeline/PipelineStatus.tsx`
  - `grep -q "Working on your video" components/pipeline/PipelineStatus.tsx`
  - `grep -q 'aria-live="polite"' components/pipeline/PipelineStatus.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck</automated>
  </verify>
  <done>Coarse pipeline UX is honest and accessible.</done>
</task>

<task type="auto">
  <name>Task 2: VideoResult component</name>
  <files>components/pipeline/VideoResult.tsx</files>
  <read_first>
  - .planning/phases/04-pipeline-integration-and-output/04-UI-SPEC.md
  </read_first>
  <action>
  Add **`components/pipeline/VideoResult.tsx`** exporting:

  ```tsx
  export function VideoResult(props: {
    videoUrl: string | null;
    jobCompleted: boolean;
    title?: string;
  }): JSX.Element
  ```

  - Section title: **`Your explainer video`** (exact from UI-SPEC).
  - Helper line: **`Play the steps below. You can fullscreen for a closer view.`** when **`videoUrl`** is non-null.
  - Container: **`aspect-video`** + **`rounded-xl`** + **`bg-card`** + padding per UI-SPEC spacing.
  - When **`videoUrl`**: **`<video controls playsInline className="h-full w-full" src={videoUrl} />`** — **no autoplay**.
  - When **`jobCompleted && !videoUrl`**: **`Alert`** variant destructive or default per spec edge case — copy: **`Video is not available yet. Refresh the page.`** (from UI-SPEC error table).
  - When **!jobCompleted && !videoUrl**: placeholder copy **`Video preview will appear here when your manual is ready.`** (exact from UI-SPEC).

  Accept optional **`ref`** forwarding to the wrapper **`div`** around **`<video>`** via **`React.forwardRef`** for scroll target (Task 4).
  </action>
  <acceptance_criteria>
  - `grep -q "Your explainer video" components/pipeline/VideoResult.tsx`
  - `grep -q "aspect-video" components/pipeline/VideoResult.tsx`
  - `grep -q '<video' components/pipeline/VideoResult.tsx` OR `grep -q "<video" components/pipeline/VideoResult.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck</automated>
  </verify>
  <done>Player and placeholders match UI contract.</done>
</task>

<task type="auto">
  <name>Task 3: Page layout — 640px upload vs 960px video band</name>
  <files>app/page.tsx</files>
  <read_first>
  - app/page.tsx
  - .planning/phases/04-pipeline-integration-and-output/04-UI-SPEC.md
  </read_first>
  <action>
  Update **`app/page.tsx`**:

  - Set **`main`** to **`max-w-[960px]`** (was `max-w-[640px]`).
  - Wrap **`<header>`** (title + tagline) in **`div className="mx-auto w-full max-w-[640px]"`**.
  - **`ManualUpload`** remains the next child; **`ManualUpload`** root should constrain the **upload card** to **`max-w-[640px] mx-auto`** while allowing **pipeline/video** sections to use **`w-full max-w-[960px] mx-auto`** inside the same component (implement constraining in Task 4 if not here).

  Ensure visual: title + upload column **640px**; video can grow to **960px** on wide screens.
  </action>
  <acceptance_criteria>
  - `app/page.tsx` contains **`max-w-[960px]`**
  - `app/page.tsx` contains **`max-w-[640px]`**
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck</automated>
  </verify>
  <done>Layout matches IA table in UI-SPEC.</done>
</task>

<task type="auto">
  <name>Task 4: Wire ManualUpload — fetch shape, polling, scroll, focus</name>
  <files>components/upload/ManualUpload.tsx</files>
  <read_first>
  - components/upload/ManualUpload.tsx
  - app/api/jobs/[id]/route.ts
  - components/pipeline/PipelineStatus.tsx
  - components/pipeline/VideoResult.tsx
  </read_first>
  <action>
  Update **`components/upload/ManualUpload.tsx`**:

  1. Extend fetch JSON type to include **`videoUrl: string | null`**, **`stages: null`**, keep **`sceneKey`**, **`status`**, **`error`**.
  2. After **`successJobId`** is set, show **`PipelineStatus`** when status is known (not only in Alert) — vertical flow: Card (upload) → **`PipelineStatus`** (if job active) → **`VideoResult`** / failure **`Alert`**.
  3. Replace inline status strings in the success **`Alert`** with **`PipelineStatus` + `VideoResult`** OR move success messaging: keep brief “Manual received” **`Alert`** if desired, but **pipeline + video** must be **below** the card per UI-SPEC (section 3 flow). Prefer removing redundant duplicate status from Alert body if **`PipelineStatus`** duplicates — goal: single pipeline block.
  4. **Failure (`status === "failed"`)**: show **`Alert` destructive** with title/copy from UI-SPEC: **`We couldn’t finish the video.`** + server **`error`** when present + **`Try uploading again`** emphasis (D-03). Secondary outline **Retry** refetch optional.
  5. **D-02**: `useRef` for video wrapper; **`useEffect`** when **`status === "completed"`** and **`videoUrl`**: **`requestAnimationFrame`** then **`ref.current?.scrollIntoView({ behavior: "smooth", block: "nearest" })`**; then **`ref.current?.querySelector("video")`** — if **`HTMLVideoElement`**, call **`.focus()`** inside **`setTimeout(..., 0)`** (guard for SSR).
  6. Poll interval: **stop on terminal** (existing) — preserve.
  7. Import **`Separator`** from shadcn only if needed — optional **`border-t`** is acceptable per UI-SPEC.

  Run **`npm run lint`** and fix new issues in touched files.
  </action>
  <acceptance_criteria>
  - `ManualUpload.tsx` imports from **`@/components/pipeline/PipelineStatus`** or relative **`../pipeline/PipelineStatus`**
  - `ManualUpload.tsx` imports **`VideoResult`**
  - `grep -q "scrollIntoView" components/upload/ManualUpload.tsx`
  - `grep -q "videoUrl" components/upload/ManualUpload.tsx`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck && npm run lint && npm run test</automated>
  </verify>
  <done>End-to-end UI reads new API and meets D-01–D-04.</done>
</task>

</tasks>
