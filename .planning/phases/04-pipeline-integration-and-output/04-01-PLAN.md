---
phase: 04-pipeline-integration-and-output
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/s3/presign.ts
  - lib/s3/presign-get.ts
  - app/api/jobs/[id]/route.ts
  - app/api/jobs/route.test.ts
autonomous: true
requirements:
  - OUTPUT-01
user_setup:
  - "AWS credentials + S3_BUCKET for presigned GET integration tests (mocked in CI)"
must_haves:
  truths:
    - "GET /api/jobs/[id] returns videoUrl when returnvalue includes a valid videoKey under uploads/{jobId}/"
    - "GET response remains backward compatible with sceneKey and adds stages: null until backend provides stages"
    - "No raw S3 secret keys appear in JSON responses"
  artifacts:
    - path: lib/s3/presign-get.ts
      provides: GetObject presign for MP4 playback
    - path: app/api/jobs/[id]/route.ts
      provides: Job status JSON including videoUrl and stages
  key_links:
    - from: worker/index.ts
      to: app/api/jobs/[id]/route.ts
      via: job.returnvalue
      pattern: videoKey
---

<objective>
Extend the **job status API** so the browser can obtain a **time-limited `videoUrl`** for HTML5 playback when the worker’s **completed** `returnvalue` includes a **`videoKey`** string (Phase 3 will populate this). Add **`stages: null`** explicitly (or omit with documented contract) so the UI can distinguish “no granular stages” from a future array. Preserve existing **`sceneKey`** behavior. Add **Vitest coverage** for new fields with **mocked** presigner and queue.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-pipeline-integration-and-output/04-CONTEXT.md
@.planning/phases/04-pipeline-integration-and-output/04-RESEARCH.md
@app/api/jobs/[id]/route.ts
@lib/s3/presign.ts
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Presigned URL for another tenant’s object | high | Only sign when `videoKey` matches `^uploads/${jobId}/` after normalizing job id from route param |
| Information disclosure via worker errors | medium | `error` field remains user-safe strings; no `failedReason` stack dumps in JSON |
| URL leakage / replay | low | Short TTL (900s); document refresh behavior in UI plan |

**ASVS:** L1 — same job-id gate as existing GET handler.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Presign GET for video object</name>
  <files>lib/s3/presign-get.ts</files>
  <read_first>
  - lib/s3/presign.ts
  - package.json
  </read_first>
  <action>
  Create **`lib/s3/presign-get.ts`** exporting:

  - `presignVideoGet(options: { key: string }): Promise<{ url: string; expiresIn: number }>` using **`GetObjectCommand`** from `@aws-sdk/client-s3`, **`getSignedUrl`** from `@aws-sdk/s3-request-presigner`, **`getS3Client()`** and **`S3_BUCKET`** from the same env pattern as `presignManualUpload`.
  - Use **`expiresIn: 900`** (match upload presign).
  - Pass **`ResponseContentType: video/mp4`** in `GetObjectCommand` when `options.key.endsWith(".mp4")`.

  Re-export or avoid duplicating **`getS3Client`** — import from `./presign` if exported, otherwise duplicate the minimal client factory consistent with `presign.ts`.
  </action>
  <acceptance_criteria>
  - File `lib/s3/presign-get.ts` exists and exports `presignVideoGet`
  - `grep -q GetObjectCommand lib/s3/presign-get.ts` exits 0
  - `grep -q expiresIn lib/s3/presign-get.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck</automated>
  </verify>
  <done>Server can mint a read URL for an S3 key.</done>
</task>

<task type="auto">
  <name>Task 2: GET /api/jobs/[id] — videoUrl and stages</name>
  <files>app/api/jobs/[id]/route.ts</files>
  <read_first>
  - app/api/jobs/[id]/route.ts
  - .planning/phases/04-pipeline-integration-and-output/04-CONTEXT.md
  </read_first>
  <action>
  Update **`app/api/jobs/[id]/route.ts`**:

  1. Parse **`returnvalue`** for optional **`videoKey`**: when `status === "completed"` and `returnvalue` is an object with **`typeof videoKey === "string"`**, validate **`videoKey === \`uploads/${id}/\` + remainder`** where remainder has **no `..`**, no backslashes, and **no leading `/`** (reuse path safety spirit from job id check). If invalid, treat as **no video** (log server-side with `console.warn` once) and set **`videoUrl: null`**.
  2. When valid, call **`presignVideoGet({ key: videoKey })`** and set **`videoUrl`** to the signed URL string. On thrown error from presign, set **`videoUrl: null`** and log **`console.error`** (do not fail the HTTP request if job is completed).
  3. Add **`stages: null`** to every successful JSON response (explicit contract for “granular stages not available”).
  4. For non-completed states, **`videoUrl`** must be **`null`**.

  Export a **TypeScript type** (e.g. `JobStatusResponse`) from this file or from a small `lib/types/job-status.ts` if the file gets crowded — at minimum document the shape in a **`/** doc comment */`** above `GET`.

  Do **not** remove **`sceneKey`** extraction logic.
  </action>
  <acceptance_criteria>
  - `app/api/jobs/[id]/route.ts` contains the string **`stages`**
  - `app/api/jobs/[id]/route.ts` contains **`videoUrl`** in the response path
  - `grep -q "uploads/\${id}" app/api/jobs/[id]/route.ts` OR validation equivalent for `uploads/` + job id prefix
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run typecheck && npm run test</automated>
  </verify>
  <done>API exposes presigned playback URL when worker supplies videoKey.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest — GET job videoUrl and stages</name>
  <files>app/api/jobs/route.test.ts</files>
  <read_first>
  - app/api/jobs/route.test.ts
  - app/api/jobs/[id]/route.ts
  </read_first>
  <action>
  In **`app/api/jobs/route.test.ts`**:

  1. Add **`vi.mock("@/lib/s3/presign-get", ...)`** exporting **`presignVideoGet: vi.fn(() => Promise.resolve({ url: "https://signed.example/video", expiresIn: 900 }))`**.
  2. Add test: completed job with **`returnvalue: { sceneKey: "uploads/x/scene.json", videoKey: "uploads/x/output.mp4" }`** → response **`videoUrl`** contains **`signed.example`**, **`stages`** is **`null`**, **`sceneKey`** still present.
  3. Add test: completed job with **`videoKey: "uploads/other/output.mp4"`** for job id **`job-done`** → **`videoUrl`** is **`null`** (or presign not called) — wrong prefix must not sign.
  4. Add test: **`queued`** / **`processing`** responses include **`videoUrl: null`** and **`stages: null`**.

  Run **`npm run test`** — all green.
  </action>
  <acceptance_criteria>
  - `app/api/jobs/route.test.ts` contains **`presignVideoGet`** or **`presign-get`** in a mock
  - `app/api/jobs/route.test.ts` contains **`videoUrl`** in at least one expectation
  - `npm run test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/janojrengaraj/Documents/CS/Assembli && npm run test</automated>
  </verify>
  <done>Regression tests lock API contract for Phase 4 UI.</done>
</task>

</tasks>
