---
phase: 02-llm-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - lib/scene/schema.ts
  - lib/scene/mock.ts
  - lib/scene/schema.test.ts
  - lib/scene/json-schema.ts
  - lib/scene/json-schema.test.ts
  - lib/scene/index.ts
  - remotion/compositions/AssemblySteps.tsx
  - .env.example
autonomous: true
requirements:
  - EXTRACT-02
  - EXTRACT-03
user_setup: []
must_haves:
  truths:
    - "Every step in mockScene includes confidence, tools, and warnings"
    - "sceneSchema.safeParse(mockScene) succeeds"
    - "npm test exits 0"
  artifacts:
    - path: lib/scene/schema.ts
      provides: Extended SceneJSON Zod source of truth
    - path: lib/scene/json-schema.ts
      provides: JSON Schema derived from sceneSchema for Claude prompts
  key_links:
    - from: lib/scene/schema.ts
      to: lib/scene/json-schema.ts
      via: zod-to-json-schema
      pattern: sceneSchema
---

<objective>
Extend the Phase 1 `sceneSchema` so each step requires **`confidence`** (number 0–1), **`tools`** (`string[]`), and **`warnings`** (`string[]`), preserving array-order step identity (D-05). Add **`zod-to-json-schema`** and a small **`lib/scene/json-schema.ts`** exporter used by Phase 2 prompting. Update **`mockScene`**, schema tests, and Remotion **`AssemblySteps`** so the composition still typechecks and renders (it may ignore `tools`/`warnings`/`confidence` until Phase 3 UX).

Purpose: Satisfy **EXTRACT-02** (single Zod SSOT) and **EXTRACT-03** (schema support for per-step confidence).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-llm-extraction/02-CONTEXT.md
@.planning/phases/02-llm-extraction/02-RESEARCH.md
@lib/scene/schema.ts
@lib/scene/mock.ts
@CLAUDE.md
</context>

<threat_model>
| Threat | Severity | Mitigation in this plan |
|--------|----------|-------------------------|
| Schema extension breaks existing jobs/templates | medium | Keep required fields backward-compatible in *data* terms: Remotion only reads title/caption/parts; new fields are additive |
| JSON Schema export drifts from Zod | medium | Generate from `sceneSchema` only; single import in `json-schema.ts` |

**ASVS:** L1 — no new trust boundaries; types and validation only.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add zod-to-json-schema dependency</name>
  <files>package.json, package-lock.json, .env.example</files>
  <read_first>
  - package.json
  - .env.example
  </read_first>
  <action>
  Run `npm install zod-to-json-schema@^3.25.2` (Zod 3 peer). Do **not** add `@anthropic-ai/sdk` in this plan (plan 02-03). Append to `.env.example` a comment-only line documenting that Claude env vars follow in later plans (no required new keys in this plan unless you add a placeholder — prefer no dummy secrets).
  </action>
  <acceptance_criteria>
  - `package.json` contains `"zod-to-json-schema"` in `dependencies` or `devDependencies`
  - `package-lock.json` updated
  - `npm ls zod-to-json-schema` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm install && npm ls zod-to-json-schema</automated>
  </verify>
  <done>Dependency installed and lockfile consistent.</done>
</task>

<task type="auto">
  <name>Task 2: Extend stepSchema and scene root</name>
  <files>lib/scene/schema.ts</files>
  <read_first>
  - lib/scene/schema.ts
  - .planning/phases/02-llm-extraction/02-CONTEXT.md
  </read_first>
  <action>
  In `stepSchema`, add:
  - `confidence: z.number().min(0).max(1)`
  - `tools: z.array(z.string())` default via `.default([])` **or** require array (empty allowed) — choose **required array** with empty allowed: `z.array(z.string())` (no `.optional()`).
  - `warnings: z.array(z.string())` same as tools.
  Keep `partSchema` unchanged. Keep root `.strict()`.
  </action>
  <acceptance_criteria>
  - `lib/scene/schema.ts` contains the substring `confidence`
  - Same file contains `tools:` and `warnings:` inside the step object schema
  - `z.array(z.string())` appears at least twice for tools and warnings
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && rg "confidence|tools:|warnings:" lib/scene/schema.ts</automated>
  </verify>
  <done>Step shape matches D-04/D-05.</done>
</task>

<task type="auto">
  <name>Task 3: Export JSON Schema for prompts</name>
  <files>lib/scene/json-schema.ts, lib/scene/json-schema.test.ts, lib/scene/index.ts</files>
  <read_first>
  - lib/scene/schema.ts
  - lib/scene/index.ts
  </read_first>
  <action>
  Create `lib/scene/json-schema.ts` that imports `sceneSchema` and exports `sceneJsonSchema` using `zodToJsonSchema(sceneSchema, { name: 'SceneJSON', $refStrategy: 'none' })` or equivalent documented options that produce a single JSON Schema object without external `$ref` files. Add `lib/scene/json-schema.test.ts` asserting `typeof sceneJsonSchema === 'object'` and `sceneJsonSchema` has top-level key **`type`** or **`properties`** (either is acceptable for a valid schema object). Re-export `sceneJsonSchema` from `lib/scene/index.ts`.
  </action>
  <acceptance_criteria>
  - File `lib/scene/json-schema.ts` exists
  - File contains `zodToJsonSchema` and `sceneSchema`
  - `lib/scene/json-schema.test.ts` exists and `npm test` runs it successfully
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>JSON Schema available for Claude system/developer prompts.</done>
</task>

<task type="auto">
  <name>Task 4: Update mock and Vitest suite</name>
  <files>lib/scene/mock.ts, lib/scene/schema.test.ts</files>
  <read_first>
  - lib/scene/mock.ts
  - lib/scene/schema.test.ts
  - lib/scene/schema.ts
  </read_first>
  <action>
  For **each** step in `mockScene`, set plausible `confidence` (e.g. `0.9`), `tools` (e.g. `["Allen key"]` or `[]`), `warnings` (e.g. `[]` or one string). Extend `schema.test.ts` to assert `sceneSchema.safeParse(mockScene).success === true` and that parsing rejects a step missing `confidence` (construct inline object).
  </action>
  <acceptance_criteria>
  - `lib/scene/mock.ts` has at least three occurrences of `confidence:` (one per step)
  - `lib/scene/schema.test.ts` includes `safeParse` failure when `confidence` is omitted
  - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>Mock and tests prove EXTRACT-02/03 schema contract.</done>
</task>

<task type="auto">
  <name>Task 5: Remotion composition still typechecks</name>
  <files>remotion/compositions/AssemblySteps.tsx</files>
  <read_first>
  - remotion/compositions/AssemblySteps.tsx
  - lib/scene/schema.ts
  </read_first>
  <action>
  No visual requirement for new fields. Ensure imports use `SceneJSON` and the file compiles. If TypeScript narrows `step` props, optionally render `step.confidence` in small muted text for developer visibility in Studio — **optional**; minimum is `npm run typecheck` green.
  </action>
  <acceptance_criteria>
  - `npm run typecheck` exits 0
  - `rg "SceneJSON" remotion/compositions/AssemblySteps.tsx` finds a match
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Remotion compiles against extended SceneJSON.</done>
</task>

</tasks>
