---
phase: 02-llm-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - lib/s3/get-object.ts
  - lib/pdf/page-count.ts
  - lib/pdf/page-count.test.ts
  - lib/pdf/preflight.ts
  - lib/pdf/preflight.test.ts
autonomous: true
requirements:
  - EXTRACT-01
user_setup:
  - "Set CLAUDE_MAX_PDF_PAGES in .env for local worker (integer, e.g. 100)"
must_haves:
  truths:
    - "Worker can fetch PDF bytes from S3 by key"
    - "PDF page count preflight throws before Claude when over limit"
    - "npm test exits 0"
  artifacts:
    - path: lib/s3/get-object.ts
      provides: GetObject helper for manual.pdf
    - path: lib/pdf/preflight.ts
      provides: Page limit guard (D-06/D-07)
  key_links:
    - from: lib/s3/presign.ts
      to: lib/s3/get-object.ts
      via: shared S3Client factory pattern
      pattern: getS3Client
---

<objective>
Add **S3 GetObject** support and **PDF page-count pre-flight** so the worker rejects oversized manuals **before** any Anthropic API call (roadmap success criterion 3, D-06/D-07). Use **`pdf-parse`** for `numpages` on the downloaded buffer.

Purpose: Cost and context guards for **EXTRACT-01** pipeline entry.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-llm-extraction/02-CONTEXT.md
@.planning/phases/02-llm-extraction/02-RESEARCH.md
@lib/s3/presign.ts
@lib/queue.ts
@CLAUDE.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| S3 credentials exfiltration via logs | high | Never log raw PDF; log only key length / job id |
| Path traversal on S3 key | medium | Reuse keys built only from `buildManualPdfKey(jobId)` pattern; validate `jobId` format if adding helpers |
| DoS via huge PDF download | medium | Optional: enforce `sizeBytes` from job payload against downloaded `ContentLength` if available |

**ASVS:** L1 — server-side object access with env credentials.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install pdf-parse</name>
  <files>package.json, package-lock.json</files>
  <read_first>
  - package.json
  </read_first>
  <action>
  Run `npm install pdf-parse@^1.1.1` (or current stable 1.x per STACK). Add `@types/pdf-parse` to devDependencies only if types are missing and needed.
  </action>
  <acceptance_criteria>
  - `package.json` lists `pdf-parse`
  - `npm ls pdf-parse` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm install && npm ls pdf-parse</automated>
  </verify>
  <done>pdf-parse available in Node worker.</done>
</task>

<task type="auto">
  <name>Task 2: S3 GetObject buffer helper</name>
  <files>lib/s3/get-object.ts</files>
  <read_first>
  - lib/s3/presign.ts
  </read_first>
  <action>
  Create `lib/s3/get-object.ts` exporting `getObjectBuffer(params: { key: string }): Promise<Uint8Array | Buffer>` using `GetObjectCommand` from `@aws-sdk/client-s3`, reusing `getS3Client()` from `presign.ts` (import from same module or extract shared client to avoid duplication — **prefer importing `getS3Client` from `./presign`** if exported, else duplicate minimal client factory matching `presign.ts`). Read body via `transformToByteArray()` or stream collect. Throw on missing `S3_BUCKET` with same pattern as presign.
  </action>
  <acceptance_criteria>
  - `lib/s3/get-object.ts` exists and contains `GetObjectCommand`
  - File contains `getObjectBuffer` export
  - `rg "getS3Client" lib/s3/get-object.ts` matches (or shared client import from presign)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Worker can load manual bytes from S3.</done>
</task>

<task type="auto">
  <name>Task 3: Page count and preflight</name>
  <files>lib/pdf/page-count.ts, lib/pdf/preflight.ts, .env.example</files>
  <read_first>
  - .planning/phases/02-llm-extraction/02-CONTEXT.md
  </read_first>
  <action>
  Implement `countPdfPages(buffer: Buffer): Promise<number>` wrapping `pdf-parse` and returning `data.numpages`. Implement `assertPdfPagePreflight(buffer: Buffer, maxPages: number): Promise<void>` in `lib/pdf/preflight.ts` that calls `countPdfPages` and throws a **plain `Error`** with message exactly: `PDF exceeds maximum page limit` when `numpages > maxPages`. Add to `.env.example`: `CLAUDE_MAX_PDF_PAGES=100` with comment: reject before Claude when PDF page count exceeds this integer.
  </action>
  <acceptance_criteria>
  - `lib/pdf/preflight.ts` contains substring `PDF exceeds maximum page limit`
  - `.env.example` contains `CLAUDE_MAX_PDF_PAGES`
  - `countPdfPages` uses `pdf-parse`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && rg "PDF exceeds maximum page limit" lib/pdf/preflight.ts && rg "CLAUDE_MAX_PDF_PAGES" .env.example</automated>
  </verify>
  <done>Preflight matches D-06 wording contract for failures.</done>
</task>

<task type="auto">
  <name>Task 4: Unit tests with mocked pdf-parse</name>
  <files>lib/pdf/page-count.test.ts, lib/pdf/preflight.test.ts</files>
  <read_first>
  - lib/pdf/preflight.ts
  - vitest.config.ts
  </read_first>
  <action>
  Use `vi.mock('pdf-parse')` to return `{ numpages: 5 }` and assert preflight passes for `maxPages: 10`. Mock `{ numpages: 200 }` and assert `assertPdfPagePreflight` rejects with message `PDF exceeds maximum page limit` when `maxPages` is `100`.
  </action>
  <acceptance_criteria>
  - `npm test` exits 0
  - `lib/pdf/preflight.test.ts` contains `PDF exceeds maximum page limit`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>Automated proof of preflight behavior without real PDF binaries.</done>
</task>

</tasks>
