---
phase: 02-llm-extraction
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - package.json
  - package-lock.json
  - .env.example
  - lib/claude/model.ts
  - lib/claude/extract-scene.ts
  - lib/claude/extract-scene.test.ts
  - lib/claude/parse-json-response.ts
  - lib/claude/parse-json-response.test.ts
autonomous: true
requirements:
  - EXTRACT-01
  - EXTRACT-02
  - EXTRACT-03
user_setup:
  - "ANTHROPIC_API_KEY in .env"
  - "CLAUDE_MODEL id string (document in .env.example)"
must_haves:
  truths:
    - "extractSceneFromPdfBuffer retries at most 2 times after Zod failure (3 total attempts)"
    - "sceneSchema.parse validates successful model output"
    - "npm test exits 0 with mocked Anthropic client"
  artifacts:
    - path: lib/claude/extract-scene.ts
      provides: Claude PDF → SceneJSON with retry loop
    - path: lib/claude/parse-json-response.ts
      provides: Fence stripping + JSON.parse
  key_links:
    - from: lib/scene/schema.ts
      to: lib/claude/extract-scene.ts
      via: safeParse / parse
      pattern: sceneSchema
---

<objective>
Implement **`lib/claude/extract-scene.ts`**: given a **PDF `Buffer`**, call **Claude Messages API** with native PDF document content, require **JSON-only** structured output matching **`sceneSchema`**, validate with **`sceneSchema.safeParse`**, and on failure retry up to **2** additional times (D-08) including **Zod `error.flatten()` or `issues` path** in the next user message. Centralize **`CLAUDE_MODEL`** and **`ANTHROPIC_API_KEY`** reading. Add **`parse-json-response`** helper that strips optional ```json fences.

Purpose: Core **EXTRACT-01/02/03** logic testable without S3 or BullMQ.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-llm-extraction/02-CONTEXT.md
@.planning/phases/02-llm-extraction/02-RESEARCH.md
@lib/scene/schema.ts
@lib/scene/json-schema.ts
@CLAUDE.md
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| API key leak | critical | Read only `process.env.ANTHROPIC_API_KEY`; never log key; redact errors |
| Prompt injection via PDF | medium | Zod strict schema rejects non-conforming output; capped retries limit abuse |
| Model returns executable content | low | Parse as JSON only; no `eval` |

**ASVS:** L1 — secrets in environment; no client exposure.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Install Anthropic SDK</name>
  <files>package.json, package-lock.json, .env.example</files>
  <read_first>
  - package.json
  - .env.example
  - CLAUDE.md
  </read_first>
  <action>
  Run `npm install @anthropic-ai/sdk@0.82.0` (pin exact **0.82.0** per CLAUDE.md STACK). Append to `.env.example`: `ANTHROPIC_API_KEY=` and `CLAUDE_MODEL=claude-sonnet-4-20250514` (or the current project-default Sonnet ID from Anthropic docs — **document one concrete string** in `.env.example`).
  </action>
  <acceptance_criteria>
  - `package.json` contains `@anthropic-ai/sdk` version `0.82.0`
  - `.env.example` contains `ANTHROPIC_API_KEY` and `CLAUDE_MODEL`
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && rg "@anthropic-ai/sdk" package.json && rg "ANTHROPIC_API_KEY|CLAUDE_MODEL" .env.example</automated>
  </verify>
  <done>SDK pinned; env template documents keys.</done>
</task>

<task type="auto">
  <name>Task 2: JSON response parser</name>
  <files>lib/claude/parse-json-response.ts, lib/claude/parse-json-response.test.ts</files>
  <read_first>
  - lib/claude/parse-json-response.ts
  </read_first>
  <action>
  Export `parseModelJson(text: string): unknown` that trims whitespace, removes optional leading/trailing markdown fences matching ```json ... ``` or ``` ... ```, then `JSON.parse`. Throw `Error` with message `Invalid JSON from model` on parse failure.
  </action>
  <acceptance_criteria>
  - `lib/claude/parse-json-response.ts` contains `Invalid JSON from model`
  - Test passes for raw JSON string and for fenced ```json\n{}\n```
  - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>Shared parse path for all attempts.</done>
</task>

<task type="auto">
  <name>Task 3: extractSceneFromPdfBuffer with retries</name>
  <files>lib/claude/model.ts, lib/claude/extract-scene.ts, lib/claude/extract-scene.test.ts</files>
  <read_first>
  - lib/scene/schema.ts
  - lib/scene/json-schema.ts
  - package.json
  </read_first>
  <action>
  Implement `extractSceneFromPdfBuffer(options: { pdfBuffer: Buffer; maxAttempts?: number }): Promise<SceneJSON>` with **default `maxAttempts = 3`** (i.e. 2 retries after first failure). Build a **system or developer** instruction that includes: emit **only** JSON matching the **SceneJSON** schema; every step must have **confidence**, **tools**, **warnings**, **title**, **caption**, **parts**; **tools** and **warnings** may be empty arrays. First user message: include PDF as **base64** in a **document** content block per `@anthropic-ai/sdk` types (`messages.create`). On `safeParse` failure, send a follow-up **user** message with string containing the substring **`Zod validation failed`** and the JSON string of `result.error.flatten()` (or `issues` array). Use `process.env.CLAUDE_MODEL` and `process.env.ANTHROPIC_API_KEY` — throw if missing with messages containing `CLAUDE_MODEL` / `ANTHROPIC_API_KEY` respectively.

  In **`extract-scene.test.ts`**, `vi.mock('@anthropic-ai/sdk')` so the first call returns invalid JSON, second returns JSON missing `confidence`, third returns `JSON.stringify(mockScene)` (extended mock from `lib/scene/mock.ts`). Assert **three** `messages.create` calls and final result equals parsed mock scene.
  </action>
  <acceptance_criteria>
  - `lib/claude/extract-scene.ts` contains `Zod validation failed`
  - `lib/claude/extract-scene.ts` contains `maxAttempts` or literal `3` attempts loop with clear comment "D-08"
  - `lib/claude/extract-scene.test.ts` asserts mock `messages.create` call count **3**
  - `npm test` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>Retry economics and validation match roadmap criterion 4.</done>
</task>

</tasks>
