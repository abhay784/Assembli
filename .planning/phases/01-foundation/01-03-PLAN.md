---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - package.json
  - remotion.config.ts
  - remotion/index.tsx
  - remotion/Root.tsx
  - remotion/compositions/AssemblySteps.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Remotion composition registers with defaultProps from validated mock SceneJSON"
    - "Step counter uses array index + 1 over steps (D-02)"
    - "Parts render from typed props (D-03), not opaque JSON"
  artifacts:
    - path: remotion/Root.tsx
      provides: Composition registration for Studio
    - path: remotion/compositions/AssemblySteps.tsx
      provides: Mock-driven slide layout
    - path: remotion.config.ts
      provides: Remotion 4 project config
  key_links:
    - from: remotion/compositions/AssemblySteps.tsx
      to: lib/scene/mock.ts
      via: defaultProps or staticImport
      pattern: mockScene|SceneJSON
---

<objective>
Remotion 4.0.445 compositions that render the Phase 1 mock `SceneJSON`: one visual “slide” per step, step counter “Step N of M” derived from **array length and index** (D-02), exploded-view-style 2D placement using structured part props (D-03). **No embedded Next.js preview** (D-04) — developers use Remotion Studio only.

Purpose: Satisfy roadmap success criterion #2 (mock → structured preview locally).

Output: `remotion/*` entrypoint, `remotion:studio` npm script, config file.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@lib/scene/schema.ts
@lib/scene/mock.ts

**Composition id:** e.g. `assembly-mock` — single composition for Phase 1.

**Dimensions:** 1920×1080, `fps` 30, `durationInFrames` derived from step count × fixed frames per step (e.g. 90 frames per step) for mock only; document constant in code comment.

**D-04:** Do not add Next.js routes for Remotion preview.
</context>

<tasks>

<task type="auto">
  <name>Task 1: remotion.config and package script</name>
  <files>remotion.config.ts, package.json, remotion/index.tsx</files>
  <action>Add `remotion.config.ts` per Remotion 4 defaults (entry: `remotion/index.tsx` or official pattern). Add `"remotion:studio": "remotion studio remotion/index.tsx"` (adjust path to match Remotion CLI expectations for this repo). Create `remotion/index.tsx` that calls `registerRoot` with `./Root`. Ensure `package.json` script is added (modify only `scripts` section).
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Script exists; entry file resolves for CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Root.tsx and AssemblySteps composition</name>
  <files>remotion/Root.tsx, remotion/compositions/AssemblySteps.tsx</files>
  <action>Register one `<Composition>` with `id="assembly-mock"` and `defaultProps={mockScene}` imported from `@/lib/scene/mock` (or relative path). Implement `AssemblySteps` as React component: props typed as `SceneJSON`. For each step index, show `title`, `caption`, overlay “Step {i+1} of {steps.length}”, render each `part` as positioned element using `x`, `y`, `rotationDeg` (simple `absolute` divs or Remotion `AbsoluteFill` children — 2D only). Use `Sequence` or `Series` so steps appear over time. No `any` for part props.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Typecheck passes; props typed from `SceneJSON`.</done>
</task>

<task type="auto">
  <name>Task 3: Remotion + React type alignment</name>
  <files>tsconfig.json, remotion/Root.tsx</files>
  <action>If needed, add a `tsconfig.remotion.json` or extend base `tsconfig` so Remotion builds resolve `@/` and JSX for `.tsx` under `remotion/`. Follow Remotion “single package” doc pattern. Fix any duplicate React types (pin single `react` version).
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npx remotion compositions remotion/index.tsx</automated>
  </verify>
  <done>CLI lists registered composition id(s) without error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev-only Studio | No auth; local developer machine |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T | Malicious mock JSON in repo | accept | Trusted dev fixtures only; runtime validation in Phase 2+ |
| T-01-03-02 | E | Accidental `npx remotion` in CI without deps | accept | Document manual Studio use; optional skip in CI |
| T-01-03-03 | I | Bundler inside Next route | mitigate | **Do not** import `@remotion/bundler` from `app/` (D-04 + Remotion Next docs) |
</threat_model>

<verification>
`npx remotion compositions remotion/index.tsx` succeeds; optional manual Studio smoke (covered in 01-06 checkpoint).
</verification>

<success_criteria>
Mock SceneJSON drives a registered composition; step order is array order; Studio-capable entrypoint exists.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
