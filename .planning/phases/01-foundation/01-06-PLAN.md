---
phase: 01-foundation
plan: 06
type: execute
wave: 5
depends_on: [01-01, 01-05]
files_modified:
  - package.json
  - components.json
  - app/globals.css
  - app/page.tsx
  - components/ui/button.tsx
  - components/ui/card.tsx
  - components/ui/progress.tsx
  - components/ui/alert.tsx
  - components/ui/alert-dialog.tsx
  - components/upload/ManualUpload.tsx
  - components/upload/magic-pdf.ts
  - components/upload/ManualUpload.test.tsx
  - lib/utils.ts
autonomous: false
requirements:
  - INGEST-01
  - INGEST-02
user_setup: []
must_haves:
  truths:
    - "User sees dropzone with exact copy from 01-UI-SPEC.md (titles, errors, 25 MB)"
    - "Client rejects wrong type, oversize, and non-%PDF- magic bytes before presign"
    - "Successful flow: POST /api/jobs → PUT to S3 → minimal success message with optional job id"
  artifacts:
    - path: components/upload/ManualUpload.tsx
      provides: Dropzone + states per UI-SPEC
    - path: app/page.tsx
      provides: Centered 640px layout per UI-SPEC
  key_links:
    - from: components/upload/ManualUpload.tsx
      to: /api/jobs
      via: fetch POST then fetch PUT presigned URL
      pattern: /api/jobs
---

<objective>
Implement the Phase 1 upload page per **01-UI-SPEC.md**: shadcn **new-york** preset, Geist, colors (`#f8fafc` page, `#0d9488` accent, destructive `#dc2626`), typography/spacing tokens, `react-dropzone` inside Card, Progress, Alert, AlertDialog for discard copy, primary/secondary CTAs with exact strings. Enforce **25 MB** and **magic-byte `%PDF-`** before calling presign (INGEST-02). Wire to **POST /api/jobs** and browser **PUT** to presigned URL (INGEST-01). **No pipeline stepper** (Phase 1 scope).

Purpose: Complete human-visible upload flow aligned with approved UI contract.

Output: `app/page.tsx` + `components/upload/*` + shadcn primitives; component test(s) for validation edge cases.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UI-SPEC.md
@lib/constants/upload.ts

**Copy (verbatim for user-visible strings):** Empty heading **Drop your assembly manual**; body **PDF only, up to 25 MB.** …; errors and success strings exactly as in UI-SPEC table; CTA **Upload assembly manual** / **Uploading…**; secondary **Choose PDF from device**; discard dialog copy from Destructive row.

**Layout:** `max-w-[640px] mx-auto`, vertical padding `py-16` md+ (`3xl` = 64px).

**shadcn blocks:** `button`, `card`, `progress`, `alert`, `alert-dialog` — official registry only.

**Magic bytes:** Read first 5 bytes of `File` via `slice(0,5)` + `arrayBuffer`; compare to `%PDF-` ASCII.

**Import `MAX_PDF_BYTES`** from `@/lib/constants/upload` for `maxSize` parity with server.
</context>

<tasks>

<task type="auto">
  <name>Task 1: shadcn init and primitives</name>
  <files>package.json, components.json, app/globals.css, lib/utils.ts, components/ui/button.tsx, components/ui/card.tsx, components/ui/progress.tsx, components/ui/alert.tsx, components/ui/alert-dialog.tsx</files>
  <action>Run `npx shadcn@latest init` with **new-york** preset (non-interactive flags as supported by CLI). Add listed components. Ensure Tailwind/theme variables match UI-SPEC accent and destructive colors (adjust CSS variables in `globals.css` if template defaults differ). Add `lucide-react` icons used by upload UI.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run build</automated>
  </verify>
  <done>Build passes; components exist under `components/ui/`.</done>
</task>

<task type="auto">
  <name>Task 2: ManualUpload + page layout + magic-byte helper</name>
  <files>components/upload/magic-pdf.ts, components/upload/ManualUpload.tsx, app/page.tsx, components/upload/ManualUpload.test.tsx</files>
  <action>Implement `magic-pdf.ts` exporting `async function isLikelyPdf(file: File): Promise<boolean>`. Implement `ManualUpload` with `react-dropzone`: `accept: { 'application/pdf': ['.pdf'] }`, `maxSize: MAX_PDF_BYTES`, all states from UI-SPEC (default, drag active, staged, uploading with Progress 0–100, success, errors). On CTA: validate → magic-byte check → `POST /api/jobs` with `{ contentType: file.type || 'application/pdf', sizeBytes: file.size }` → `fetch(uploadUrl, { method: 'PUT', body: file, headers })`. Use exact copy strings from 01-UI-SPEC.md. Implement discard **AlertDialog** with specified copy. `app/page.tsx`: title **Assembli** (or product name from PROJECT if preferred — use **Assembli** as display heading per brand) with one-line hint, then ManualUpload. Apply 640px centered column and spacing tokens.

Add `ManualUpload.test.tsx`: mock `fetch`; assert oversize and wrong extension/MIME paths show correct error strings; magic-byte failure shows **This file does not look like a valid PDF.**
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test -- components/upload/ManualUpload.test.tsx</automated>
  </verify>
  <done>Tests pass; UI matches copy and color roles from UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — Remotion Studio and upload flow</name>
  <files>app/page.tsx, remotion/index.tsx</files>
  <action>N/A — human gate after automation in Tasks 1–2.</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && node .claude/get-shit-done/bin/gsd-tools.cjs verify-path-exists app/page.tsx</automated>
  </verify>
  <done>Human confirms checklist below.</done>
  <what-built>Upload page per 01-UI-SPEC; API wired for presign + S3 PUT; Remotion mock composition from 01-03.</what-built>
  <how-to-verify>
    1. Terminal A: `npm run dev` — open http://localhost:3000 — confirm layout (640px column, colors, typography), dropzone copy, reject a non-PDF and a file &gt;25 MB with exact error strings, stage a real small PDF and confirm magic-byte path accepts.
    2. With API env configured (or mocked), run upload: confirm **Manual received.** (or success copy) after PUT completes; optional: verify job id monospace display.
    3. Terminal B: `npm run remotion:studio` — confirm `assembly-mock` (or registered id) renders mock steps with correct “Step N of M” and part positions (D-04).
    4. Optional: against real S3 dev bucket with CORS, confirm browser PUT succeeds from localhost:3000.
  </how-to-verify>
  <resume-signal>Type **approved** or list issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser UI | Untrusted files; client checks are UX only — server validates on presign |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06-01 | T | Client bypasses magic-byte check | mitigate | Server validation on POST already enforces MIME + size; S3 object is untrusted until Phase 2 scan |
| T-01-06-02 | I | XSS via filename display | mitigate | Render filename as text; do not use `dangerouslySetInnerHTML` |
| T-01-06-03 | E | Repeated upload clicks | mitigate | Disable CTA while uploading; single in-flight PUT |
| T-01-06-04 | S | Leak presigned URL via UI | accept | URL short-lived; shown only in network tab; same-origin page |
</threat_model>

<verification>
`npm test -- components/upload/ManualUpload.test.tsx`, `npm run build`, then human checkpoint Task 3.
</verification>

<success_criteria>
UI matches 01-UI-SPEC; INGEST-01/02 satisfied end-to-end with server as source of truth; human signs off on Studio + upload.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
