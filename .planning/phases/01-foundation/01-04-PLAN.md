---
phase: 01-foundation
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - package.json
  - worker/index.ts
  - lib/queue.ts
  - lib/queue.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "BullMQ worker process starts and processes a stub job when Redis is up"
    - "Queue name matches ASSEMBLI_QUEUE_NAME in lib/constants/upload.ts"
    - "Job data payload excludes file bytes (metadata only)"
  artifacts:
    - path: lib/queue.ts
      provides: Shared Queue factory and connection for API + worker
    - path: worker/index.ts
      provides: Separate Node process entry (not Next.js)
    - path: lib/queue.test.ts
      provides: Unit tests with mocked Redis/ioredis or connection-less helpers
  key_links:
    - from: worker/index.ts
      to: lib/queue.ts
      via: same Queue name and Redis URL
      pattern: ASSEMBLI_QUEUE
---

<objective>
Scaffold the **BullMQ worker** as a standalone process (`tsx worker/index.ts`) and shared queue connection module for the Next.js API producer in 01-05. Processor is a stub (e.g. log + mark complete or noop) — no Remotion bundler, no S3 fetch in Phase 1 beyond optional log of `s3Key`.

Purpose: Honor architecture constraint: heavy pipeline runs outside Next.js; establish Redis-backed job identity for upload flow.

Output: `dev:worker` script, `lib/queue.ts`, `worker/index.ts`, and tests that do not require live Redis where possible (use BullMQ mocks or skip integration with `describe.skip` documented).
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@lib/constants/upload.ts

**Job payload shape (metadata only):** `{ jobId: string, s3Key: string, contentType: string, sizeBytes: number }` — never embed PDF bytes (01-RESEARCH Pitfall 4).

**Env:** `REDIS_URL` (required), load via `dotenv/config` at top of `worker/index.ts` for local dev.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared queue module and npm dev:worker script</name>
  <files>lib/queue.ts, package.json, worker/index.ts</files>
  <action>Create `lib/queue.ts` exporting `getRedisConnection()` (ioredis from `REDIS_URL`), `ASSEMBLI_QUEUE` string from `lib/constants/upload.ts` (`ASSEMBLI_QUEUE_NAME`), and `getJobQueue()` returning `new Queue(ASSEMBLI_QUEUE, { connection })`. Add `"dev:worker": "tsx worker/index.ts"` to `package.json`. Implement `worker/index.ts`: import `dotenv/config`, create `Worker` with same queue name, stub processor that `console.log`s job id + s3Key and returns. Use `connection` options suitable for BullMQ 5 + ioredis. Guard: if `REDIS_URL` missing, print clear error and `process.exit(1)`.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Typecheck passes; worker file parses; script exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for queue name and payload shape helpers</name>
  <files>lib/queue.test.ts, lib/queue.ts</files>
  <behavior>
    - Exported queue name equals `ASSEMBLI_QUEUE_NAME` constant
    - Helper or inline validation: payload object must contain string `jobId`, `s3Key`, `contentType`, and finite `sizeBytes`
  </behavior>
  <action>Add pure functions in `lib/queue.ts` e.g. `parseJobPayload(data: unknown)` using Zod or manual guards for the metadata shape; test without Redis. Keep Redis client construction separate so tests do not open sockets.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test -- lib/queue.test.ts</automated>
  </verify>
  <done>Tests pass; no live Redis required for `npm test`.</done>
</task>

<task type="auto">
  <name>Task 3: Optional Redis integration note in .env.example</name>
  <files>.env.example</files>
  <action>Ensure `.env.example` documents `REDIS_URL=redis://127.0.0.1:6379` example and one-line comment that worker + API require it. If already present from 01-01, extend only if missing worker mention.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && grep -q REDIS_URL .env.example</automated>
  </verify>
  <done>`.env.example` mentions Redis for worker/API.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Redis | Internal job store; must not be exposed publicly in production (dev uses localhost) |
| Worker process | Trusts job data shape — validate at enqueue time (01-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | E | Redis DoS via huge payloads | mitigate | Enforce small JSON payload only; no file bytes in job.data (01-RESEARCH) |
| T-01-04-02 | T | Forged job data | mitigate | API is sole producer in Phase 1; validate payload with Zod before `queue.add` in 01-05 |
| T-01-04-03 | I | Unauthenticated Redis in prod | accept | POC; document firewall/VPC requirement for production |
| T-01-04-04 | R | Worker crash on bad message | mitigate | Stub processor wrapped; log errors; BullMQ retries configurable later |
</threat_model>

<verification>
`npm test -- lib/queue.test.ts`, `npm run typecheck`, and manual `docker compose up -d redis && REDIS_URL=redis://127.0.0.1:6379 npm run dev:worker` (short run) when Redis available.
</verification>

<success_criteria>
Worker and queue module exist; queue name centralized; tests pass without Redis socket from unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
