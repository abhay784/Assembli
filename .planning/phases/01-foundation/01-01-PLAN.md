---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
  - tsconfig.json
  - vitest.config.ts
  - docker-compose.yml
  - .env.example
  - .gitignore
  - eslint.config.mjs
  - app/layout.tsx
  - app/page.tsx
  - app/globals.css
  - lib/constants/upload.ts
  - lib/constants/upload.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "npm test runs Vitest with exit code 0"
    - "next build completes for Next.js 15"
    - "Redis is defined for local BullMQ via docker compose"
  artifacts:
    - path: vitest.config.ts
      provides: Vitest runner configuration
    - path: lib/constants/upload.ts
      provides: Shared 25 MB cap and PDF content-type constants
    - path: docker-compose.yml
      provides: Local Redis for BullMQ
  key_links:
    - from: package.json
      to: vitest.config.ts
      via: npm test script
      pattern: '"test":'
---

<objective>
Greenfield bootstrap: Next.js 15 (pinned, not 16), TypeScript, Vitest (`npm test`), shared upload constants (25 MB per 01-UI-SPEC.md), Docker Compose for Redis, ESLint + `tsc --noEmit`, and a minimal App Router shell. Add Phase 1 dependencies in one `package.json` to reduce later churn; **do not** add `remotion:studio` or `dev:worker` scripts here (plans 01-03 and 01-04 add them when `remotion/` and `worker/` exist).

Purpose: Establish the repo baseline and automated tests per 01-RESEARCH.md Validation Architecture.

Output: Runnable `npm run dev`, `npm test`, `npm run build`, and documented env vars in `.env.example`.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@CLAUDE.md

**Pins:** `next@15` (not 16), `react`/`react-dom` per Next peers, `remotion@4.0.445`, `@remotion/cli@4.0.445`, `zod@^3.25` (not v4), `bullmq`, `ioredis`, `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `react-dropzone`, `uuid`, `tsx`, `dotenv`, `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 app, dependencies, and core scripts</name>
  <files>package.json, package-lock.json, next.config.ts, tsconfig.json, .gitignore, app/layout.tsx, app/page.tsx, app/globals.css</files>
  <action>Create a Next.js 15 App Router project (greenfield). Pin `next` to 15.x. Install in one pass: `remotion@4.0.445`, `@remotion/cli@4.0.445`, `zod@^3.25`, `bullmq`, `ioredis`, `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `react-dropzone`, `uuid`, `tsx`, `dotenv`, `vitest`, `@vitejs/plugin-react`, `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`. Scripts in this plan only: `dev`, `build`, `start`, `test` (vitest run), `lint`, `typecheck`. Use Geist via `next/font` in `app/layout.tsx` per 01-UI-SPEC.md. `app/page.tsx` is a minimal placeholder (e.g. title only); full upload UI comes in 01-06. Do not create `remotion/` or `worker/` entries yet (avoids package.json script pointing at missing files).
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm install && npm run build</automated>
  </verify>
  <done>Production build succeeds; lockfile resolves `next@15` (not 16).</done>
</task>

<task type="auto">
  <name>Task 2: Vitest, upload constants, Redis compose, env template</name>
  <files>vitest.config.ts, lib/constants/upload.ts, lib/constants/upload.test.ts, docker-compose.yml, .env.example</files>
  <action>Add `vitest.config.ts` with `environment: 'node'`, include `**/*.{test,spec}.ts` under `lib/` (and later `app/` if needed). Align `@/*` with `tsconfig` paths. In `lib/constants/upload.ts` export: `MAX_PDF_BYTES = 25 * 1024 * 1024` (01-UI-SPEC.md), `ALLOWED_PDF_CONTENT_TYPES` including `application/pdf`, `ASSEMBLI_QUEUE_NAME = 'assembli-jobs'`. Add `docker-compose.yml`: Redis 7, port 6379, bind 127.0.0.1. `.env.example`: `REDIS_URL`, `AWS_REGION`, `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `S3_BUCKET`, optional `AWS_S3_ENDPOINT` for S3-compatible backends. Add `lib/constants/upload.test.ts` asserting `MAX_PDF_BYTES === 26214400` and that `application/pdf` is allowed.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test</automated>
  </verify>
  <done>`npm test` passes; constants match 25 MB cap; env template lists all keys for later plans.</done>
</task>

<task type="auto">
  <name>Task 3: ESLint and typecheck scripts</name>
  <files>package.json, eslint.config.mjs</files>
  <action>Add ESLint flat config suitable for Next 15 + TypeScript; `npm run lint` and `npm run typecheck` (`tsc --noEmit`). Fix any issues on the minimal scaffold only.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Lint and typecheck exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation | Secrets in `.env` (gitignored); `.env.example` has no real credentials |
| npm registry | Dependencies pulled at install time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | I | npm supply chain | mitigate | Pin majors (Next 15, Zod 3, Remotion 4.0.445); commit lockfile |
| T-01-01-02 | I | Secret leak via git | mitigate | `.gitignore` `.env*`; example file placeholders only |
| T-01-01-03 | E | Local Redis exposed | accept | Dev-only; bind localhost; document not for untrusted networks |
</threat_model>

<verification>
`npm test`, `npm run build`, `npm run typecheck`, and `npm run lint` succeed; `docker compose config` succeeds when Docker is installed.
</verification>

<success_criteria>
Vitest runs; upload limits defined once; Redis compose ready; Next 15 app builds.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
