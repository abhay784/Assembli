---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - lib/scene/schema.ts
  - lib/scene/mock.ts
  - lib/scene/index.ts
  - lib/scene/schema.test.ts
autonomous: true
requirements:
  - INGEST-02
user_setup: []
must_haves:
  truths:
    - "sceneSchema.safeParse passes for representative mock SceneJSON"
    - "Invalid fixtures fail Zod with clear paths"
    - "TypeScript type SceneJSON is z.infer only (no duplicate interface)"
  artifacts:
    - path: lib/scene/schema.ts
      provides: Single source of truth Zod schema (D-01–D-03)
    - path: lib/scene/mock.ts
      provides: Valid mock SceneJSON for Remotion and tests
    - path: lib/scene/schema.test.ts
      provides: Automated schema regression tests
  key_links:
    - from: lib/scene/schema.ts
      to: lib/scene/mock.ts
      via: mock satisfies inferred type
      pattern: sceneSchema
---

<objective>
Lock the Phase 1 **SceneJSON** contract: minimal Zod schema (D-01), step order = array index only (D-02), structured per-step parts with fixed typed fields (D-03). Export `SceneJSON` via `z.infer` only. Provide a representative mock document and Vitest coverage.

Purpose: Satisfy roadmap success criterion #4 and unblock Remotion templates (01-03) without duplicating TypeScript interfaces.

Output: `lib/scene/*` module consumed by Remotion and (later) the worker/API.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@lib/constants/upload.ts

**Locked decisions:** D-01 minimal fields; D-02 no per-step string id; D-03 no opaque `z.record` for visuals — use explicit part shape (e.g. `id`, `label`, `x`, `y`, `rotationDeg`).

**INGEST-02 trace:** File-type/size validation for PDF uploads is enforced in API/UI plans; this plan establishes the **same validation discipline** for structured JSON (Zod) at the scene boundary, satisfying INGEST-02’s “validate before processing” pattern for scene-shaped inputs.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement sceneSchema, mockScene, and barrel export</name>
  <files>lib/scene/schema.ts, lib/scene/mock.ts, lib/scene/index.ts</files>
  <action>Implement `sceneSchema` with `steps: z.array(stepSchema)`. Each step: `title`, `caption`, `parts: z.array(partSchema)` where `partSchema` uses explicit fields per D-03 (e.g. `id`, `label`, `x`, `y`, `rotationDeg` — all required scalars, no opaque record). Do **not** add a string `id` field on steps (D-02). Use `z.object({...}).strict()` at the root `sceneSchema` so unknown keys fail (LLM boundary prep). Export `type SceneJSON = z.infer<typeof sceneSchema>`. Add `mockScene` in `mock.ts` with ≥3 steps, each ≥1 part, furniture-assembly-like titles/captions. `index.ts` re-exports schema, type, and mock.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm run typecheck</automated>
  </verify>
  <done>Typecheck passes; mock imports only from Zod-inferred types.</done>
</task>

<task type="auto">
  <name>Task 2: Vitest coverage for valid and invalid SceneJSON</name>
  <files>lib/scene/schema.test.ts</files>
  <action>Add tests: (1) `mockScene` passes `sceneSchema.safeParse`; (2) missing `steps` fails; (3) invalid part numeric type fails; (4) extra top-level key fails when using strict root schema.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test -- lib/scene/schema.test.ts</automated>
  </verify>
  <done>All four behaviors asserted; `npm test` green.</done>
</task>

<task type="auto">
  <name>Task 3: Document step indexing contract (D-02)</name>
  <files>lib/scene/schema.ts</files>
  <action>Add a brief module comment: step display index = array index + 1 over `steps`; no separate step ids in Phase 1.
</action>
  <verify>
    <automated>cd /Users/abhaykorlapati/Desktop/Assembli && npm test -- lib/scene/schema.test.ts</automated>
  </verify>
  <done>Comment present; tests still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Future LLM output → schema | Untrusted JSON must be validated before render (Phase 2+) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | T | Malformed scene JSON | mitigate | Zod rejection; strict object parsing; tests lock behavior |
| T-01-02-02 | E | Oversized JSON DoS | accept | Phase 1 uses small mocks; Phase 2 adds size limits on API body |
| T-01-02-03 | I | Dual TS/Zod schema drift | mitigate | **Only** `z.infer` for types (01-RESEARCH.md Don’t Hand-Roll) |
</threat_model>

<verification>
`npm test -- lib/scene/schema.test.ts` and `npm run typecheck` pass.
</verification>

<success_criteria>
Single Zod schema; mock validates; tests cover valid + invalid paths; D-01–D-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
